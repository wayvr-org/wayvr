<layout>
  <elements>
    <div
      consume_mouse_events="1"
      id="root"
      new_pass="1"
      width="100%"
      height="100%"
      flex_direction="column"
      position="absolute"
    >
      <!-- Top bar -->
      <rectangle
        position="relative"
        color="background_contrast"
        width="100%" height="48"
      >

        <!-- Shine effect at the top -->
        <rectangle position="absolute" width="100%" height="3" round="4" color="highlight(opacity-0.1)" />

        <!-- Top bar contents -->
        <div gap="16" align_items="center">
          <!-- Back button -->
          <Button id="but_back" width="48" height="48" color="on_background_contrast(transparent)" border_color="on_background_contrast(transparent)">
            <sprite src_builtin="dashboard/back.svg" width="24" height="24" color="on_background_contrast" />
          </Button>

          <!-- Title -->
          <label id="popup_title" color="on_background_contrast" weight="bold" size="18" />
        </div>
      </rectangle>

      <rectangle
        width="100%"
        height="100%"
        color="background_contrast(opacity-0.98)"
        color2="background(opacity-0.99)"
        gradient="vertical"
        position="relative"
      >
        <div id="content" width="100%" height="100%" position="absolute" overflow_y="scroll">
          <!-- Padding set at runtime -->
          <!-- Content, filled-in at runtime -->
        </div>      
      </rectangle>
    </div>
  </elements>
</layout>
